# Spark-era copies of pathway filenames (not canonical)

These files were copied from `~/fourcastnet/docs/research/` on spark-61dd.
**Sheldon’s numbered pack** (`docs/00-pathway` … `docs/04-gates`) is the canonical research set.
Do not promote these snapshots over that pack. Kept only so the Spark tree is fully archived.
