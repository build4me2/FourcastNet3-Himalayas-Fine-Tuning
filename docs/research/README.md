# Spark operational docs

Live tree on spark-61dd: `~/fourcastnet/docs/`.

This folder holds **Howard/Leonard operational** notes copied from Spark (`CLAUDE.md`, `REFERENCE.md`, `PROGRESS.md`, gate reports).

- **Canonical research pack** stays in `docs/00-pathway` … `docs/04-gates` (Sheldon). Do not overwrite those from Spark copies.
- Leonard call notes: `docs/06-calls/`
- Spark-era snapshots of colliding filenames: `docs/05-eng/research/` (not canonical)
- Small gate JSON: `docs/06-metrics/` and `runs/phase0/`
